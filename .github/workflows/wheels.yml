name: Build Python Wheels

on:
  push:
   branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-wheels:
    name: Build wheels for ${{ matrix.os }}
    runs-on: ${{ matrix.runs-on }}
    strategy:
      matrix:
        include:
          - os: linux-intel
            runs-on: ubuntu-latest
            arch: x86_64
          - os: linux-arm
            runs-on: ubuntu-24.04-arm
            arch: aarch64
          - os: windows-intel
            runs-on: windows-latest
            arch: AMD64
          - os: windows-arm
            runs-on: windows-11-arm
            arch: ARM64
          - os: macos-intel
            runs-on: macos-15-intel
            arch: x86_64
            version: "13.0"
          - os: macos-arm
            runs-on: macos-14
            arch: arm64
            version: "14.0"
    steps:
    - uses: actions/checkout@v6
      with: 
        persist-credentials: false

    - uses: pypa/cibuildwheel@v3.4.1
      env:
        CIBW_BEFORE_BUILD_MACOS: brew install libomp
        # CIBW_BEFORE_BUILD_LINUX: yum install -y libgomp || apt-get install -y libgomp1
        CIBW_ARCHS_MACOS: "${{ matrix.arch }}"
        CIBW_ARCHS_LINUX: "${{ matrix.arch }}"
        CIBW_ARCHS_WINDOWS: "${{ matrix.arch }}"
        CIBW_ENVIRONMENT_MACOS: MACOSX_DEPLOYMENT_TARGET=${{ matrix.version }}
        CIBW_SKIP: "pp* *-win32 *_i686"
    - uses: actions/upload-artifact@v4 
      with: 
        name: cibw-wheels-${{ matrix.os }} 
        path: ./wheelhouse/*.whl 
  
  build-sdist:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
        - uses: actions/checkout@v6
          with: 
            persist-credentials: false
        
        - name: build sdist
          run: pipx run build --sdist

        - uses: actions/upload-artifact@v4
          with:
            name: cibw-sdist
            path: dist/*.tar.gz

  upload-pypi:
    needs: [build-wheels, build-sdist]
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/download-artifact@v5
        with: 
            pattern: cibw-*
            path: dist
            merge-multiple: true

      - uses: pypa/gh-action-pypi-publish@release/v1
        # To test uploads to TestPyPI, uncomment the following:
        with:
          repository-url: https://test.pypi.org/legacy/